"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const mongoose = require("mongoose");
const HEX_COLOR_CODE = /^#([A-Fa-f0-9]{6}|[A-Fa-f0-9]{3}|[A-Fa-f0-9]{8})$/;
const validate = (value) => {
    if (typeof value !== "string") {
        throw mongoose.SchemaType.CastError("HexColorCode", `Value is not string: ${value}`);
    }
    if (!HEX_COLOR_CODE.test(value)) {
        throw mongoose.SchemaType.CastError("HexColorCode", `Value is not a valid HexColorCode: ${value}`);
    }
    return value;
};
class HexColorCode extends mongoose.SchemaType {
    constructor(key, options) {
        super(key, options, "HexColorCode");
    }
    cast(val) {
        validate(val);
        return val;
    }
}
HexColorCode.instance = "HexColorCode";
mongoose.Schema.Types.HexColorCode = HexColorCode;
exports.default = HexColorCode;
