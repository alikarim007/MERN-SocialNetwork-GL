"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const mongoose = require("mongoose");
const validate = (value) => {
    const UTC_OFFSET_REGEX = /^([+-]?)(\d{2}):(\d{2})$/;
    if (typeof value !== "string") {
        throw mongoose.SchemaType.CastError("UtcOffset", `Value is not string: ${value}`);
    }
    if (!UTC_OFFSET_REGEX.test(value)) {
        throw mongoose.SchemaType.CastError("UtcOffset", `Value is not a valid UTC Offset: ${value}`);
    }
    return value;
};
class UtcOffset extends mongoose.SchemaType {
    constructor(key, options) {
        super(key, options, "UtcOffset");
    }
    cast(val) {
        validate(val);
        return val;
    }
}
UtcOffset.instance = "UtcOffset";
mongoose.Schema.Types.UtcOffset = UtcOffset;
exports.default = UtcOffset;
